# 雪球热股榜抓取 — 安装与定时运行说明

## 1. 安装依赖

```bash
pip install -r requirements.txt
playwright install chromium
```

## 2. 立即运行一次

```bash
python xueqiu_scraper.py
```

结果保存在 `xueqiu_data/xueqiu_hot_YYYY-MM-DD.csv`

---

## 3. 定时运行（每天自动抓取）

### 方式 A：脚本内置调度（最简单）

```bash
# 默认每天 15:10（A股收盘后）运行
python xueqiu_scraper.py --daemon

# 自定义时间
python xueqiu_scraper.py --daemon --time 09:35
```

保持终端运行，建议配合 `nohup`：

```bash
nohup python xueqiu_scraper.py --daemon > xueqiu.log 2>&1 &
```

---

### 方式 B：macOS cron（系统级稳定）

```bash
crontab -e
```

添加（每天 15:10 工作日运行，路径换成你自己的）：

```
10 15 * * 1-5 /usr/bin/python3 /Users/你的用户名/xueqiu_scraper.py >> /Users/你的用户名/xueqiu.log 2>&1
```

---

### 方式 C：macOS launchd（开机自启，最稳定）

创建 `~/Library/LaunchAgents/com.xueqiu.scraper.plist`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.xueqiu.scraper</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/python3</string>
        <string>/Users/你的用户名/xueqiu_scraper.py</string>
    </array>
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>   <integer>15</integer>
        <key>Minute</key> <integer>10</integer>
    </dict>
    <key>StandardOutPath</key>
    <string>/Users/你的用户名/xueqiu.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/你的用户名/xueqiu_err.log</string>
</dict>
</plist>
```

加载：

```bash
launchctl load ~/Library/LaunchAgents/com.xueqiu.scraper.plist
```

---

## 4. 反爬排查

| 现象 | 原因 | 解决 |
|------|------|------|
| 返回 400/403 | Cookie 过期 | 脚本每次重新获取，通常自愈 |
| 数据为空 | 雪球修改了 API 路径或字段 | 打印 `resp.json()` 确认结构 |
| 行业字段全空 | quote 接口字段名变更 | 在 `enrich_industry()` 中 `print(q.keys())` 确认字段 |
| Playwright 超时 | 网络慢 | 增大 `timeout` 或临时改 `headless=False` 调试 |

---

## 5. 字段投资参考

| 字段 | 意义 |
|------|------|
| 排名变化 | 正=上升；连续多天上升 = 关注度聚集信号 |
| 雪球关注人数 | 散户情绪先行指标，常领先价格 1-3 天 |
| 成交额(亿) | 验证热度真实性——无量的热榜通常是虚热 |
| 行业 | 同行业多股同时上榜 = 板块轮动强信号 |
| 换手率(%) | 高换手 + 上涨 = 强势；高换手 + 下跌 = 出货警惕 |
